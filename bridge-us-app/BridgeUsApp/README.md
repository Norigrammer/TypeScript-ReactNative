# BridgeUsApp (初期テンプレート)

このディレクトリは `create-expo-app` で生成された初期テンプレートです。

**現在のメインアプリケーションは親ディレクトリ（`bridge-us-app/`）にあります。**

## 開発を始める場合

親ディレクトリに移動してください:

```bash
cd ..
npm install
npx expo start
```

詳細は [../README.md](../README.md) を参照してください。
